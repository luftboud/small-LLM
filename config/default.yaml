# All the numbers and paths a run needs, in one place — nothing below should be
# hardcoded in src/. Override any key from the CLI, e.g.:
#   python -m model_origin train --override model.random_forest.n_estimators=300
